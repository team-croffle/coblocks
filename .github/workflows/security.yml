name: Security

on:
  push:
    branches: ['**']
    tags-ignore: ['**']
  pull_request:
  schedule:
    # 매주 월요일 09:00 KST (00:00 UTC)
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 검증된(verified) 시크릿만 빌드를 세운다.
  # unknown 까지 켜면 .env.example 의 placeholder DSN 이나 개발 시드 비밀번호가 매번 걸려
  # 경보가 무의미해진다. 제외 경로는 .github/trufflehog-exclude.txt 에 이유와 함께 적는다.
  secrets:
    name: TruffleHog secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # PR 은 base..head 차분만 보므로 전체 히스토리가 필요하다.
          fetch-depth: 0

      # PR: 이 PR 이 새로 들여온 커밋만 검사한다. 빠르고, 남의 과거 커밋으로 실패하지 않는다.
      - name: Scan pull request range
        if: github.event_name == 'pull_request'
        uses: trufflesecurity/trufflehog@main
        with:
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
          extra_args: --results=verified --exclude-paths=.github/trufflehog-exclude.txt

      # 그 외(브랜치 푸시·정기·수동): 작업 트리 전체를 검사한다.
      - name: Scan working tree
        if: github.event_name != 'pull_request'
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          extra_args: --results=verified --exclude-paths=.github/trufflehog-exclude.txt

  deps:
    name: Dependency review
    # PR 에서만 동작하는 액션이다.
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          comment-summary-in-pr: on-failure
          fail-on-severity: high
